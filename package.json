{
  "name": "perp-position-subgraph",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "codegen": "graph codegen",
    "build": "npm run codegen && graph build",
    "deploy": "npm run build && graph deploy perpetual-protocol/perp-position-subgraph --access-token ${THE_GRAPH_ACCESS_TOKEN} --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/",
    "create-local": "graph create perp-local-subgraph --node http://127.0.0.1:8020",
    "deploy-local": "graph deploy perp-local-subgraph --ipfs http://localhost:5001 --node http://127.0.0.1:8020"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
